arij.db
arij.db-wal
arij.db-shm
e2e.db
e2e.db-wal
e2e.db-shm
sessions/
logs/
uploads/
migrations/
arij.db.audit37-backup*
# Dated snapshots taken before risky operations. Unignored, they land in
# `git status` AND in Tailwind's source scan — a 20 MB SQLite file full of
# ticket text yields bogus utility candidates that fail the CSS build.
arij.db.backup-*
# Ownership manifest for lib/mcp/user-global-sync.ts: the third-party MCP
# server names Arij last wrote into omp's mcp.json and agy's register. Machine
# state, not project state — it records what is on THIS box.
mcp-user-global.json
backups/
